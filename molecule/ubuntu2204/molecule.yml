---
# Molecule configuration for Ubuntu 22.04 (Jammy Jellyfish)
driver:
  name: delegated
  options:
    managed: false
    ansible_connection_options:
      ansible_connection: ssh
      ansible_ssh_user: root
      ansible_python_interpreter: /usr/bin/python3

platforms:
  - name: ubuntu2204
    # Platform identifier for documentation

provisioner:
  name: ansible
  playbooks:
    prepare: ../default/prepare.yml
    converge: ../default/converge.yml
    verify: ../default/verify.yml
  inventory:
    hosts:
      all:
        hosts:
          ubuntu2204:
            ansible_host: localhost
            ansible_user: root
            ansible_python_interpreter: /usr/bin/python3
            target_os: ubuntu
            target_os_version: "22.04"

scenario:
  name: ubuntu2204
  test_sequence:
    - dependency
    - cleanup
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - side_effect
    - verify
    - cleanup
    - destroy
